# Project Working Agreement

## Source of truth

- EDSS 개방데이터 원본과 `data/metadata/`의 수집기록을 기준으로 판단한다.
- 대학알리미 API 자료는 최신 갱신과 교차검증에 사용한다.
- 제공목록에 적힌 연도 범위와 실제 다운로드 파일의 연도 범위를 각각 기록한다.

## Data handling

- `data/raw/`의 원본을 덮어쓰거나 수정하지 않는다.
- 학교코드, 캠퍼스코드, 학과코드는 앞자리 0을 보존하는 문자열로 읽는다.
- 원본 열 이름은 보존하고, 표준화한 이름은 별도 매핑표에서 관리한다.
- 결측치, 비공개값, 해당 없음, 0을 서로 구분한다.
- 다운로드·변환 결과마다 행 수, 열 수, 중복키, 체크섬을 검증한다.

## Git policy

- 원본 및 대용량 정제 데이터는 Git에 커밋하지 않는다.
- 스크립트, 설정, 데이터 사전, 수집기록, 품질검사 결과만 커밋한다.
- 인증키, 쿠키, 세션 정보, 개인정보는 저장하지 않는다.

